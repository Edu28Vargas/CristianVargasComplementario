<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CristianVargasComplementario.Camara">
    <ContentPage.Content>
        <StackLayout   BackgroundColor="#f6f6f6" >
            <Frame BackgroundColor="#fff" Margin="8">
                <StackLayout>

                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60"/>
                         
                        </Grid.RowDefinitions >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                       
                        <Button  Grid.Column="0" Text="Tomar Foto"
                                x:Name="btntokePhoto" 
                                Clicked="btntokePhoto_Clicked"/>
                        <Image Grid.Row="1" Grid.Column="1" HorizontalOptions="End" x:Name="Photo"
                       Aspect="Fill"  Margin="0,10,0,0"/>
                    </Grid>
                </StackLayout>
            </Frame>

         
        </StackLayout>
    </ContentPage.Content>
</ContentPage>